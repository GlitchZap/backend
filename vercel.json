{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.x"  // Replace with the version you need, e.g., "3.9" or "3.10"
    },
    "commands": {
      "install": "pip install -r requirements.txt",
      "build": "python main.py"
    }
  }
}
